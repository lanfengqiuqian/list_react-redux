import React from 'react'

class Key extends React.Component {
    constructor(props) {
        super(props);
        this.state = {

        }
        this.numHandle = this.numHandle.bind(this);
    }

    numHandle(num) {
        // this.props.numHandle(num);
        console.log(2)
    }

    render() {
        return (
        );
    }
}

export default Key;

